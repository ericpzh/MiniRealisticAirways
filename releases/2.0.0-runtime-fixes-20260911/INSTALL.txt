MiniRealisticAirways 2.0.0 - normal runtime-fixes build (2026-09-11)

Close Mini Airways. Back up the existing MiniRealisticAirways.dll, then copy
this DLL to Mini Airways/BepInEx/plugins/MiniRealisticAirways.dll.
Do not install the source ZIP or compilation references as plugins.

Normal event scheduling is restored to 1800 +/- 300 scaled game seconds.
Event duration and applicability/retry rules remain unchanged. The debug
runway hotkeys and on-screen test panel are absent from this build.

Runway closure: airborne approaches to that runway go around; aircraft already
touched down complete landing. Taking-off aircraft continue. New landing and
takeoff commands are rejected while closed. Stock disabled markers are used.
Discord connection retries use 600 unscaled seconds; activity retries retain
5 seconds, with connection recreation after three consecutive failures.

The user reported the focused in-game runway test passed. Automated policy and
Discord-state regressions and a Release build are also checked. This does not
claim comprehensive platform, long-session, or live Discord verification.

Build source: dotnet build MiniRealisticAirways.csproj -c Release
State tests: dotnet run --project tools/DiscordRuntimeStateTest/DiscordRuntimeStateTest.csproj
The state test project targets .NET 10. The plugin targets netstandard2.1.
The source package contains the existing repository compilation references.
