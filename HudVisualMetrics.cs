namespace MiniRealisticAirways;

// Aircraft and waypoint HUDs use the same font and level-block dimensions.
internal static class HudVisualMetrics
{
    internal const float FontSize = 5f;
    internal const float BlockSize = 0.28f;
    internal const float BlockGap = 0.12f;
}
